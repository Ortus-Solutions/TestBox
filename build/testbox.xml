<?xml version="1.0"?>
<!-- ====================================================================== 
     TestBox Build (www.coldbox.org)                                                     
     ====================================================================== -->
<project name="testbox-build" default="build" basedir="../">
	<description>
    	Build a new distribution of TestBox
    </description>
	<!-- Version: UPDATE ON EACH RELEASE AS NEEDED -->
	<property name="testbox.version"	value="2.0.0"/>
	<property name="testbox.slug"		value="testbox"/>
	<!-- Build Labels -->
	<tstamp prefix="start"/>
	<!-- Load Contrib Tasks -->
	<path id="cp">
		<fileset dir="build/lib">
			<include name="**/*.jar" />
		</fileset>
	</path>
	<!-- Define Tasks -->
	<taskdef resource="net/sf/antcontrib/antlib.xml" classpathref="cp" />
	
	<!-- Init -->
	<target name="init" description="Init">
		<!-- Default environment check, if not passed via -Denvironment -->
    	<condition property="environment" value="local">
    		<not><isset property="environment" /></not>
    	</condition>
    	<echo>Loading from environment: ${environment}</echo>
		<!-- Load env properties -->
		<loadproperties srcFile="build/testbox-${environment}.properties"/>
		<!-- Build Number -->
		<propertyfile file="build/build.number" comment="Build Number for ANT. Edit not!">
			<entry key="build.number" 
					type="int" 
			     	operation="+"
					pattern="00000"
			     	default="1" />
		</propertyfile>
		<property file="build/build.number"/>
		<!-- Build Label -->
		<property name="build.label" value="testbox-${testbox.version}.${build.number}-${start.DSTAMP}${start.TSTAMP}"/>
		<!-- Cleanup + Init -->
		<delete dir="${dir.build}" />
		<mkdir dir="${dir.build}/testbox/apidocs"/>
		<chmod file="${dir.build}/**" perm="g+wxrs" type="both" />
	</target>
	
	<!-- Build test reports -->
    <target name="run-tests">
        <subant target="run-junit">
          	<fileset dir="tests" includes="test.xml"/>
    		<property name="environment" value="${environment}" />
        </subant>
    </target>
	
	<!--build-->
	<target name="build" description="Build a new TestBox Distribution" depends="init,run-tests">
      	
		<!-- Copy build ID -->
		<concat destfile="${dir.build}/testbox/${build.label}">Built on ${start.TODAY}</concat>
		
		<!-- Copy SRC to TestBox namespace -->	
      	<copy todir="${dir.build}/testbox/">
        	<fileset dir=".">
        		<exclude name="build/**" />
        		<exclude name="apidocs/**" />
        		<exclude name="tests/**" />
        		<exclude name="cpu.json" />
        		<exclude name="box.json" />
        		<exclude name="box-mockbox.json" />
        		<exclude name=".*/**" />
        		<exclude name="settings.xml" />
        		<exclude name="build-testbox/**" />
        	</fileset>
        </copy>

        <!-- Copy box.json -->	
      	<copy file="box.json" toFile="${dir.build}/box.json" />
        
        <!-- Copy Samples -->
		<copy todir="${dir.build}/testbox/samples">
        	<fileset dir="tests/" />
        </copy>
		
		<!-- Replace Build Numbers -->
		<replaceregexp match='@build.number@' replace="${build.number}" flags="ig" byline="true">
		  <fileset dir="${dir.build}">
		  </fileset>
		</replaceregexp>
						
		<!-- Execute ColdDoc -->
		<get dest="${dir.build}/colddoc.html" src="${url.api}${testbox.version}&amp;path=${dir.api}&amp;testbox_root=${dir.build}/testbox" verbose="true"/>
		<delete file="${dir.build}/colddoc.html" />
		
		<!-- Zip API Docs -->
		<zip destfile="${dir.exports}/testbox_apidocs_${testbox.version}.zip" basedir="${dir.build}/testbox/apidocs"></zip>
		<!-- Zip Bundle -->
		<zip destfile="${dir.exports}/testbox_${testbox.version}.zip" basedir="${dir.build}"></zip>
		<!-- Build Checksum -->
		<checksum forceoverwrite="true" fileext=".md5">
			<fileset dir="${dir.exports}">
				<include name="*.zip" />
			</fileset>
		</checksum>

		<!-- Perms for automated builds -->
		<chmod file="${dir.build}/**" perm="g+wxrs" type="both" />

		<!-- Bleeding edge, always stored in root of integration server -->
		<if>
			<equals arg1="${be.name}" arg2="" />
			<then></then>
			<else>
				<delete>
					<fileset dir="${be.exports}">
						<include name="${be.name}*" />
					</fileset>
				</delete>
				<copy file="${dir.exports}/testbox_${testbox.version}.zip" tofile="${be.exports}/${be.name}.zip" />
				<copy file="${dir.exports}/testbox_${testbox.version}.zip.md5" tofile="${be.exports}/${be.name}.zip.md5" />
			</else>
		</if>

		<!-- Cleanup -->
		<delete dir="${dir.build}" />
				
    </target>
	
</project>